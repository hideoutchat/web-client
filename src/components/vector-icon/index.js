import styled from 'styled-components';

const VectorIcon = styled.svg.attrs({ fill: 'currentColor', viewBox: '0 0 256 256' })`
  height: 1em;
  margin: auto;
  width: 1em;
`;

export default VectorIcon;
