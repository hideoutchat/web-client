import styled from 'styled-components';

const ReactionList = styled.div`
  align-items: center;
  display: flex;
  flex-direction: row;
  margin-left: 48px;
  padding: 0 10px;
`;

export default ReactionList;
